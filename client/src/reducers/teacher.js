//import some actions

const teacher = (state = {}, action) => {
  switch (action.type) {
    // case START_REQUEST:
    //   return {
    //     isFetching: true,
    //     ...state
    //   };

    default:
      return state;
  }
};
export default teacher;
